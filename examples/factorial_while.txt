handing over to you |..-. .- -.-. -| (|-.|)
{
    |.- -.-.| encoding matches |-.|;

    reseption if (|-.| match |-----|)
    {
        out |.----|;
    };

    reseption while (|-.| diverse |.----|)
    {
        |-.| encoding matches |-.| - |.----|;
        |.- -.-.| encoding matches |-.| * |.- -.-.|;
    };

    out |.- -.-.|;
}


handing over to you |-- .- .. -. | ()
{
    give a signal (|-.|);
    
    |..-.| encoding matches |..-. .- -.-. -| (|-.|);

    get the message (|..-.|) ;
}
